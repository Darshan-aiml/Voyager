"""Playwright stub entrypoint for RedBus browser automation."""


def run() -> dict[str, str]:
    return {
        "provider": "RedBus",
        "status": "stub",
        "message": "Implement Playwright browser steps for bus listing search, seat map selection, traveller details, OTP pause, and payment handoff.",
    }
